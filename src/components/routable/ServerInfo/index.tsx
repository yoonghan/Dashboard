import * as React from "react";
import SystemDetailInformation from "../../container/SystemDetailInformation";

const ServerInfo: React.SFC<any> = () => {
  return (
    <SystemDetailInformation/>
  );
};

export default ServerInfo;
